# Copy to .env on the secured VM. Never commit the real values.
PGHOST=internal-postgres-host
PGPORT=5432
PGDATABASE=gisdb
PGUSER=vacant_land_refresh_ro
PGPASSWORD=replace-with-read-only-password
TOLEMI_EXPORT_CONCURRENCY=20
